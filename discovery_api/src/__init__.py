"""Endpoint 2 — personalised discovery feed. Source root (scaffold; no logic yet).

Reuses the shared substrate over HTTP: vector :8000, graph :8010 (see ../../shared/README.md).
Build order (later prompts): data_access -> ranking -> feed/carousels -> why -> API.
"""
